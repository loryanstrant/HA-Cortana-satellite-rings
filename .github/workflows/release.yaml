name: "Release"

on:
  release:
    types:
      - published
permissions:
  contents: write
  packages: write


jobs:
  release:
    name: Prepare release
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Create zip file
        run: |
          cd custom_components/cortana_rings
          zip -r ../../cortana_rings.zip . -x "__pycache__/*" "*.pyc" "*.pyo"

      - name: Upload zip to release
        uses: svenstaro/upload-release-action@v2
        with:
          repo_token: ${{ secrets.GITHUB_TOKEN }}
          file: ./cortana_rings.zip
          asset_name: cortana_rings.zip
          tag: ${{ github.event.release.tag_name }}
          overwrite: true
